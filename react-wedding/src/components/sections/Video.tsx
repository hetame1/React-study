import classNames from 'classnames/bind'
import styles from './Heading.module.scss'

import Section from '../shared/Section'

const cx = classNames.bind(styles)

const Video = () => {
  return <Section>fs</Section>
}

export default Video
